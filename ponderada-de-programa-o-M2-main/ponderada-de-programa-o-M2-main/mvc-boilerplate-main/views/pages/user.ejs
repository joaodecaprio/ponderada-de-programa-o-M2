<!DOCTYPE html>

<html lang="pt-br">
<head>
  
  <meta charset="UTF-8">
  <title>Usuários</title>
  <style>
    body, html {
      background: #18182f !important;
      color: #fff !important;
      min-height: 100vh;
      height: 100%;
      margin: 0;
      padding: 0;
    }

    .styled-table th, .styled-table td, h1, h2, label, p, a {
      color: #fff !important;
    }

    .styled-table {
      border-collapse: separate;
      border-spacing: 0;
      font-size: 1.05em;
      background: #23234b;
      border-radius: 22px;
      overflow: hidden;
      box-shadow: 0 8px 32px #7f5cff33;
      border: 4px solid #7f5cff;
    }

    .styled-table thead tr {
      background: linear-gradient(90deg,#7f5cff,#43e97b);
      color: #fff;
      text-align: left;
      font-weight: bold;
    }

    .styled-table th,
    .styled-table td {
      padding: 14px 18px;
      border: none;
    }

    .styled-table tbody tr {
      background: #29294d;
      transition: background 0.2s;
    }

    .styled-table tbody tr:nth-of-type(even) {
      background: #23234b;
    }

    .styled-table tbody tr:last-of-type {
      border-bottom: 2px solid #7f5cff;
    }

    .styled-table tbody tr:hover {
      background-color: #18182f;
    }

    .container {
      max-width: 1100px;
      margin: 0 auto;
      padding: 20px;
      min-height: 100vh;
      display: flex;
      flex-direction: row;
      align-items: flex-start;
      gap: 40px;
      background: #18182f; /* Fundo igual ao cabeçalho */
    }

    .table-area {
      flex: 1.2;
      min-width: 320px;
      max-width: 400px;
      align-self: flex-start;
    }

    .image-container {
      flex: 1.8;
      display: flex;
      align-items: flex-start;
      justify-content: flex-end;
      margin-top: -40px;
      margin-left: auto;
    }

    .image-container img {
      width: 100%;
      max-width: 650px;
      min-width: 320px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.13);
      border-radius: 12px;
      object-fit: cover;
    }
  </style>
</head>
<body>
  <%- include('../components/header') %>
  <div class="container">
    <div class="table-area">
      <div class="page-header" style="margin-top: 0; margin-bottom: 10px;">
        <h1 style="color:#fff;">Usuários</h1>
        <p class="subtitle" style="color:#fff;">Veja todos os usuários cadastrados!</p>
      </div>
      <h2 style="text-align:center; color:#7f5cff; margin-bottom: 10px;">Lista de Usuários</h2>
      <table class="styled-table" style="width: 100%; font-size: 0.98em;">
        <thead>
          <tr style="background: linear-gradient(90deg,#7f5cff,#43e97b); color: #fff;">
            <th style="padding: 12px 8px; font-weight: 600; border: none;">Nome</th>
            <th style="padding: 12px 8px; font-weight: 600; border: none;">Email</th>
          </tr>
        </thead>
        <tbody>
          <% users.forEach(function(user, idx) { %>
            <tr style="background: <%= idx % 2 === 0 ? '#29294d' : '#23234b' %>;">
              <td style="padding: 10px 8px; color: #fff; border: none;"><%= user.name %></td>
              <td style="padding: 10px 8px; color: #bdbdfc; border: none;"><%= user.email %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <!-- Imagem à direita e no topo -->
    <div class="image-container">
      <img src="/megamind.png" alt="Descrição da imagem">
    </div>
  </div>
</body>
</html>