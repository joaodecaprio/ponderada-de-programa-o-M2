<h1>Lista de Categorias</h1>
<table class="styled-table">
  <thead>
    <tr>
      
      <th>Categorias</th>
      <th></th>
    </tr>
  </thead>                                                                                                          
  <tbody>
    <% categories.forEach(function(category) { %>
    
      <tr>
        <td><%= category.name %>
        <td><%= category.description ? category.description : '' %></td>
      </tr>
    <% }) %>
  </tbody>
</table>
<div class="form-container">
  <h2>Associar Categoria a uma Tarefa</h2>
  <form action="/task" method="POST">
    <label for="taskName">Nome da Tarefa:</label>
    <input type="text" id="taskName" name="taskName" required>
    

    <label for="categoryId">Categoria:</label>
    <select id="categoryId" name="categoryId" required>
      <option value="">Selecione uma categoria</option>
      <% categories.forEach(function(category) { %>
        <option value="<%= category.id %>"><%= category.name %></option>
      <% }) %>
    </select>

    <button type="submit">Criar Tarefa</button>
  </form>

    <%- include('../layout/task') %>
</div>


<div style="display: flex; justify-content: flex-end; gap: 16px; margin-bottom: 18px; align-items: flex-start; margin-top: -400px;">
  <img src="/gelado1.jpg" alt="Descrição da imagem" style="width: 300px; height: auto; border-radius: 10px; box-shadow: 0 2px 8px rgba(74,144,226,0.13);">
  <img src="/gelado2.jpg" alt="Descrição da imagem" style="width: 315px; height: auto; border-radius: 10px; box-shadow: 0 2px 8px rgba(74,144,226,0.13);">
</div>



